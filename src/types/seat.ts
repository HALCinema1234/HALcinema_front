// ===============================================================
// GET seats/{上映管理ID}
// ===============================================================
// 映画のリストを取得
export type TSeat = {
    row: string; // 行
    col: number; // 列
    state?: 'vacant' | 'reserved' | 'selected'; // 状態
};
